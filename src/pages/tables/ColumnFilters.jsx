import React from "react";
import PreviewLayout from "../../layouts/PreviewLayout";
import ColumnFiltersPreview from "../../components/tables/column-filters/ColumnFiltersPreview";

const ColumnFilters = () => {
  return <PreviewLayout preview={<ColumnFiltersPreview />} />;
};

export default ColumnFilters;
