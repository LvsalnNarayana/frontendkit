import React from "react";
import PreviewLayout from "../../layouts/PreviewLayout";

const InfiniteScroll = () => {
  return <PreviewLayout preview={<>infinite scroll</>} />;
};

export default InfiniteScroll;
