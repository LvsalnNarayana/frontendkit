import React from "react";
import PreviewLayout from "../../layouts/PreviewLayout";

const EditableData = () => {
  return <PreviewLayout preview={<>editable data</>} />;
};

export default EditableData;
