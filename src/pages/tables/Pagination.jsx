import React from 'react'
import PreviewLayout from '../../layouts/PreviewLayout'

const Pagination = () => {
  return (
    <PreviewLayout preview={<>pagination</>}/>
  )
}

export default Pagination
